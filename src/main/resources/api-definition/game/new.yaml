post:
  summary: Initialize a new game
  operationId: initializeNewGame
  tags:
    - game
  responses:
    200:
      description: The ID of the game
      content:
        application/json:
          schema:
            type: string
